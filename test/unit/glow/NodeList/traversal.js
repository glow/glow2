function byId(id) {
	return document.getElementById(id);
}

module('glow.NodeList traversal');

test('glow.dom.NodeList#parent', 1, function() {
	equal(typeof glow.NodeList, 'function', 'glow.NodeList is function');
	
});

test('glow.dom.NodeList#prev', 1, function() {
	equal(typeof glow.NodeList, 'function', 'glow.NodeList is function');
	
});

test('glow.dom.NodeList#next', 1, function() {
	equal(typeof glow.NodeList, 'function', 'glow.NodeList is function');
	
});

test('glow.dom.NodeList#get', 1, function() {
	equal(typeof glow.NodeList, 'function', 'glow.NodeList is function');
	
});

test('glow.dom.NodeList#ancestors', 1, function() {
	equal(typeof glow.NodeList, 'function', 'glow.NodeList is function');
	
});

test('glow.dom.NodeList#children', 1, function() {
	equal(typeof glow.NodeList, 'function', 'glow.NodeList is function');
	
});

test('glow.dom.NodeList#contains', 1, function() {
	equal(typeof glow.NodeList, 'function', 'glow.NodeList is function');
	
});